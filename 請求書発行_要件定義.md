# 請求書発行機能（v2）要件定義

> 前提ドキュメント：[受注調整_要件定義.md](受注調整_要件定義.md)（v1）
> v1で「①受注 ＋ ②調整（赤伝／貸方）」の台帳を整えた。v2は**③請求書**を実装し、①②を集計・相殺して発行する。

## ✅ 目的・位置づけ

**顧客×期間の受注を集計し、調整（CREDIT）を控除して、適格請求書（インボイス）を発行・記録する。**
v1で作った調整台帳を「そのまま食う」形で組む。北極星（3層構造）はv1で確定済み：

```
① Order（受注）        … 税込・不変。何を届けいくら請求するか
        ＋
② OrderAdjustment（調整）… 赤伝／貸方。CREDIT（請求相殺）とCASH（現金返金）を区別
        ↓ 集計（v2で実装）
③ Invoice（請求書）     … 顧客×期間で Σ①受注 −Σ②CREDIT ＋税 を発行・記録・PDF
```

## ✅ 現状把握（v1完了時点・コード確認済み）

- **請求書は未実装。** 帳票は納品書・領収書のみ（`orders/pdf.py` / WeasyPrintでHTML→PDF）。
- 金額は**税込（内税）保持**。消費税は `合計 ÷ (1+税率)` を `ROUND_DOWN` で割戻し（`pdf.py:52-56`）。税率は `OrderSettings.tax_rate`（既定8%＝軽減税率運用）。
- `OrderSettings.invoice_number = "T4020002059715"`（適格請求書発行事業者の登録番号）保持済み → **v2は適格請求書として発行**。
- `Customer.invoice_timing`（IMMEDIATE即時／MONTH_END月末締め）確定。v1で受注フォームと連動済み。
- `Order.billing_pattern`（MONTH_END／IMMEDIATE）**v1で記録開始済み**。顧客既定をプリフィル・受注ごとに上書き可。← v2の「漏れ検知」の土台。
- `OrderAdjustment`：`settlement`（CREDIT請求相殺／CASH現金返金）、`occurred_on`（帰属日）、税込 `amount`。← v2の控除行の材料。
- まだ無いもの：**請求書レコード・請求書番号採番・請求状況（請求済み判定）・発行後ロック**。v2で新規に作る。

## ✅ スコープ

### v2でやること（今回）
- **請求書レコードの発行**（Invoice / InvoiceLine）＝顧客×対象受注の確定スナップショット
- **3つの発行導線**（軸は2つ）：
  - ① 月末締め（定期）：対象月の未請求受注を一括
  - ② 即時発行（都度・1件）：受注詳細から1件を請求
  - ③ スポットまとめ（都度・N件）：一覧で複数選択して1枚に束ねる
- **集計エンジン**：Σ受注合計 − 紐づくCREDIT調整 ＝ net、税率別内訳を割戻し
- **繰越クレジット**：発行済み受注に後発したCREDIT調整を、次回請求書に控除行として繰越
- **適格請求書PDF**（WeasyPrint・T番号・税率別内訳・控除行）
- **発行後ロック**：請求書に取込んだ受注・調整を編集/削除不可に
- **請求状況の可視化**：受注一覧に「未請求／請求済み」列、ダッシュボードに**「要即時請求」ウィジェット**（IMMEDIATE未請求の件数バッジ）
- **請求書一覧・詳細・再発行・取消（VOID）・入金フラグ（PAID）**
- 請求書番号の採番（連番）

### v2でやらないこと（→ v3）
- **入金消込の自動化・督促（催促状）** → v3
- 会計ソフト連携・電子帳簿保存対応の自動アーカイブ → v3
- 決済ゲートウェイ／自動引落 → 対象外
- 承認ワークフロー（発行前承認）→ 必要なら v3

## ✅ 実装詳細：モデル（v2）

### 1) orders.models.Invoice（請求書ヘッダ）

```python
class Invoice(models.Model):
    STATUS_DRAFT = 'DRAFT'      # 下書き（金額はスナップショット済み・未確定）
    STATUS_ISSUED = 'ISSUED'    # 発行済み（顧客に出した・ロック）
    STATUS_PAID = 'PAID'        # 入金済み（手動フラグ・消込自動化はv3）
    STATUS_VOID = 'VOID'        # 取消（無効化・受注のロック解除）
    STATUS_CHOICES = [(STATUS_DRAFT,'下書き'),(STATUS_ISSUED,'発行済み'),
                      (STATUS_PAID,'入金済み'),(STATUS_VOID,'取消')]
    PATTERN_CHOICES = [('MONTH_END','月末締め'),('IMMEDIATE','即時/スポット')]

    invoice_number   = CharField(unique=True)          # 請求書番号（採番・T登録番号とは別物）
    customer         = FK(Customer, on_delete=PROTECT, related_name='invoices')
    pattern          = CharField(choices=PATTERN_CHOICES)   # どの導線で作ったか（記録）
    period_start     = DateField(null=True)            # 対象期間開始（月末締め＝当月1日 / 都度＝任意）
    period_end       = DateField(null=True)            # 対象期間終了（月末締め＝当月末日）
    issue_date       = DateField()                     # 発行日
    due_date         = DateField(null=True)            # 支払期日
    status           = CharField(choices=STATUS_CHOICES, default=STATUS_DRAFT)

    # ── 発行時点のスナップショット（後で設定が変わっても請求書は不変）──
    registration_number = CharField()   # OrderSettings.invoice_number(T番号)を固定保存＝適格請求書
    tax_rate         = DecimalField(5,2)  # 発行時点の税率（軽減税率変更に備え固定）
    charge_total     = DecimalField(10,0) # 対象受注の税込合計
    credit_total     = DecimalField(10,0) # 控除するCREDIT調整の税込合計（繰越含む）
    net_amount       = DecimalField(10,0) # = charge_total − credit_total（税込・請求額）
    tax_amount       = DecimalField(10,0) # net_amountの内税割戻し（÷(1+税率), ROUND_DOWN）

    notes            = TextField(blank=True)
    issued_by        = FK(AUTH_USER_MODEL, on_delete=SET_NULL, null=True)
    created_at / updated_at
```

### 2) orders.models.InvoiceLine（請求明細＝日別の注文明細を1行ずつ）★日別明細対応

```python
class InvoiceLine(models.Model):
    SOURCE_ORDER = 'ORDER'
    SOURCE_ADJUSTMENT = 'ADJUSTMENT'
    SOURCE_CHOICES = [(SOURCE_ORDER,'受注'),(SOURCE_ADJUSTMENT,'調整（控除）')]

    invoice     = FK(Invoice, on_delete=CASCADE, related_name='lines')
    source_type = CharField(choices=SOURCE_CHOICES)
    order       = FK(Order, on_delete=PROTECT, null=True, blank=True)         # 受注行の元受注
    order_item  = FK(OrderItem, on_delete=SET_NULL, null=True, blank=True)     # 元の受注明細（追跡用）
    adjustment  = FK(OrderAdjustment, on_delete=PROTECT, null=True, blank=True) # 調整（控除）行
    line_date   = DateField()          # 納品日 or 調整発生日（明細に日付を出す＝日別）
    label       = CharField()          # 商品名スナップショット
    quantity    = PositiveIntegerField(null=True, blank=True)  # 注文数（数量なし明細＝控除/仕出し等はNULL）
    unit_price  = DecimalField(10,0, null=True, blank=True)    # 単価（税込・同上でNULL可）
    amount      = DecimalField(10,0)   # 金額（税込）。受注=正 / 調整控除=負
    sort_order  = PositiveIntegerField(default=0)
```

- **なぜ明細で受注/調整を持つか**：請求書は「発行時点の内容の写し」。あとで受注が消えても請求書PDFを再現できるよう、`label`/`quantity`/`unit_price`/`amount`/`line_date` をスナップショットする（FKは追跡用・PROTECTで実体も守る）。

#### 明細の粒度（＝日別の注文明細）★今回の追加要件
- **保存（InvoiceLine）は明細1行ずつ**（OrderItem／OrderExtraItem＝1行）でスナップショットし、監査可能性を保つ。
- **表示（請求書PDF・詳細・プレビュー）は「1日1行」に集計**する（`services.aggregate_day_rows`）。同一日に価格帯が複数あっても**1行にまとめ**、注文数＝食数合計・金額＝当日合計。
- **単価は内訳（segments）で表示**：当日を単価ごとに分け、`¥500×8個 / ¥600×2個` のように単価列にスタック表示（単一価格の日は1行）。単価が取れない明細（仕出し等）は個数のみ／空欄。
- **月末締め（period あり）は対象月の全日付（1日〜末日）＋曜日を並べる**。受注の無い日も空行で出す（配達実績を1枚で確認できる弁当屋の月次請求書の定番レイアウト）。土日は淡色＋曜日を赤字。
- 即時/スポット（period なし）は受注のあった日付だけを並べる。
- 列：**日・曜日・摘要・注文数・単価・金額**。
- **数量/単価が取れない明細**（仕出し弁当：`quantity=0` で小計直接／自由額調整）は数量・単価を空欄にし**金額のみ**。
- 控除（CREDIT調整）は日別集計に潰さず、発生日＋曜日で1件ずつ**マイナス行**（△表記）として明細の下に並べる。

### 3) 既存モデルへの seam 追加（v1で予告済み・nullable FK＝安全マイグレーション）

```python
# Order に追加
class Order:
    invoice = FK('Invoice', on_delete=SET_NULL, null=True, blank=True,
                 related_name='orders')   # この受注が載っている（有効な）請求書。未請求ならNULL
    # → 「1受注は最大1枚の有効請求書」を担保（二重請求防止）。VOID時にNULLへ戻す。

# OrderAdjustment に追加
class OrderAdjustment:
    billed_invoice = FK('Invoice', on_delete=SET_NULL, null=True, blank=True,
                        related_name='adjustments')  # このCREDIT調整を取込んだ請求書
    # → 発行後ロック＋繰越の二重取込防止。CASHは常にNULL（請求書に載らない）。
```

- どちらも **nullable FK追加＝既存データに影響しない安全マイグレーション**（v1要件定義§将来で予告済み）。

### 4) Order プロパティ／マネージャ追加（DB変更なし）

```python
Order.is_invoiced        # bool: invoice_id が有効な（VOID以外の）請求書を指すか
Order.invoice_status_label  # 「未請求／請求済み／取消済み」表示
# クエリ用
Order.objects.uninvoiced()          # invoice IS NULL（未請求）
Order.objects.pending_immediate()   # billing_pattern=IMMEDIATE かつ 未請求（要即時請求ウィジェット用）
```

### 5) OrderUserMenuPermission に `can_view_invoices`（既定True）を追加

## ✅ 集計ロジック（発行エンジンの中核）

**選択された受注群 + それに紐づく未取込CREDIT調整**を1枚に束ねる。汎用の集計関数を1本用意し、①②③がそれを再利用する（v1の月次相殺サマリーもこの特殊ケース）。

```
build_invoice(customer, orders:list, extra_credits:list=None, *, issue_date, period, due_date):
    1. orders は「その顧客の・未請求（invoice IS NULL）」だけを許可（それ以外はエラー）
    2. charge_total = Σ order.total（税込）
    3. credits = 対象受注に紐づくCREDIT調整（billed_invoice IS NULL）
                 ＋ extra_credits（繰越：過去請求済み受注に後発したCREDIT）
       credit_total = Σ credit.amount
    4. net_amount = charge_total − credit_total
    5. tax_amount = net_amount − (net_amount ÷ (1+税率))  ※ROUND_DOWN・pdf.pyと同一式
    6. InvoiceLine を生成（日別明細）：
       - 受注ぶん(+)：各 order の items / extra_items を1行ずつ展開。
         line_date=order.delivery_date, label=商品名, quantity, unit_price, amount=subtotal。
         納品日→表示順でソート（sort_orderに連番）。仕出し等で数量/単価が無い行はNULL・金額のみ。
       - 調整ぶん(−)：CREDIT調整を1行ずつ。line_date=occurred_on, amount=−調整額,
         quantity/unit_price=NULL, label=「【控除】種別・理由要約」。
    7. Invoice を DRAFT で保存し、registration_number・tax_rate をスナップショット
    ── 発行(ISSUED)確定時 ──
    8. 対象 order.invoice = this / 対象 credit.billed_invoice = this をセット（ロック）
       ※トランザクションで一括。競合（同時に別請求へ）を防ぐため select_for_update。
```

- **CASH返金は集計に一切入れない**（請求書に載らない・別集計のまま）。
- **税率別内訳**：現状は全品8%運用のため単一税率でOK。将来10%品が混ざる場合に備え、`tax_rate` をスナップショットしておき、明細集計を税率でグルーピングできる余地を残す（v2実装は単一税率、拡張点だけ確保）。

## ✅ 発行の3導線（軸は2つ・スポットは発行時操作）

| # | 導線 | 対象受注の選び方 | 画面 |
|---|---|---|---|
| ① | 月末締め（定期） | 顧客×対象月[1日〜末日]の未請求受注を**自動全選択** | 月次発行画面：月＋顧客（or 全MONTH_END顧客一括）→ プレビュー → 発行 |
| ② | 即時発行（都度1件） | その1受注だけ | 受注詳細「この受注を請求」ボタン → プレビュー → 発行 |
| ③ | スポットまとめ（都度N件） | 未請求受注一覧でチェック選択 | 受注一覧で複数チェック → 「まとめて請求」→ プレビュー → 発行 |

- ②③は同一エンジン。`billing_pattern=IMMEDIATE` の受注を1件で出せば②、複数束ねれば③。
- ①は `billing_pattern=MONTH_END` を主対象にしつつ、**実際の判定は「未請求受注の選択」**（billing_patternは初期選択の目安・強制しない）。

## ✅ 繰越クレジット（適格返還請求書／貸方の実務）

- 既に**請求済みの受注**に後から発生したCREDIT調整（`billed_invoice IS NULL` かつ元受注が請求済み）は、**過去の発行済み請求書を書き換えない**。
- 次回その顧客の請求書を作るとき、`extra_credits` として**単独の控除行（マイナス）で繰越**して取込む。
- 繰越候補は発行画面で**「繰越調整あり（N件・−¥XXX）」と明示**し、取込む/持ち越すを選べる。
- CASHは対象外（返金済み・請求書に載せない）。

## ✅ 請求番号の採番

- 形式（案）：`INV-YYYYMM-####`（例 `INV-202607-0001`）。月内連番・ゼロ埋め4桁。
- `OrderSettings.invoice_number`（T番号）とは**完全に別物**（あちらは登録番号・請求書に併記）。
- 採番は発行（DRAFT→ISSUED）確定時に確定。DRAFTは仮番号 or 未採番。
- → **形式は要決定事項**（下記）。

## ✅ 画面（URL）と挙動（v2）

`orders/urls.py` に追加：

1. **請求書一覧** `invoices/`：期間・顧客・ステータスで絞り込み、合計、CSV。
2. **月次発行** `invoices/issue/monthly/`：対象月＋顧客（全MONTH_END顧客一括も可）→ 未請求受注と繰越調整をプレビュー → 発行。
3. **スポット発行** `invoices/issue/spot/`：受注一覧からチェックした受注群を受けてプレビュー → 発行。
4. **即時発行** 受注詳細に「この受注を請求」ボタン → プレビュー → 発行（②）。
5. **請求書詳細** `invoices/<pk>/`：明細（受注＋控除）・税内訳・ステータス操作（発行/入金/取消）・PDF。
6. **請求書PDF** `invoices/<pk>/pdf/`：適格請求書。
7. **取消（VOID）** `invoices/<pk>/void/`：ロック解除（order.invoice=NULL / credit.billed_invoice=NULL）。理由必須。
8. **受注一覧（既存に追記）**：**請求状況列**（未請求／請求済み）＋スポット選択チェックボックス。
9. **ダッシュボード（既存に追記）**：**「要即時請求」ウィジェット**＝ `pending_immediate()` を件数バッジ付き一覧。

## ✅ 適格請求書PDF（記載要件）

WeasyPrintでHTML→PDF（既存 `pdf.py`／`receipt.html` と同方式）。適格請求書として最低限：

- 発行事業者名・住所（`OrderSettings`）＋**登録番号（T番号）**
- 請求先（顧客名・部署）
- 請求書番号・発行日・支払期日・対象期間
- **日別の注文明細（表）**：`納品日｜商品名｜注文数｜単価｜金額`（税込）を日付順に列挙。
  - 数量/単価が無い明細（仕出し・自由額）は数量・単価を空欄にし金額のみ。
  - 控除行（調整）は `発生日｜【控除】理由要約｜（数量・単価空欄）｜−金額`。
- **税率ごとの対価の合計額と消費税額**（現状8%単一）
- 請求金額合計（税込 net_amount）
- 振込先・支払方法（`Customer.payment_method`）

## ✅ 明細レイアウト v2.1：単価別マトリクス表示（今回の変更）★確定

### 狙い
日別明細を、**単価を列にしたクロス集計（マトリクス）表**にする。行＝日付、列＝単価（例 ¥650 / ¥500 / ¥400）、セル＝その日その単価の**注文数**。弁当屋の月次請求書の定番。1日1行は維持。

### 確定した仕様（2026-07-29）
- **単価列の並び順：降順（高い順）** 650→500→400。
- **摘要列：残す**（「お弁当」）。
- **追加商品・仕出しは金額列に混ぜず「追加」列（金額）に分離**。
- **大盛りは基本単価に混ぜず「大盛り(+¥50)」列（個数）に分離**。基本単価列にはその弁当を通常個数として1個計上し、大盛りぶんの上乗せ（+50等）は大盛り列に個数で立てる。→ `金額 ＝ Σ(基本単価×数量) ＋ (大盛り上乗せ×大盛り数) ＋ 追加` で必ず整合する。
  - 大盛り上乗せ単価は `OrderItem` の `subtotal − 単価×数量` から算出（＝No.11「大盛りごはん」単価。顧客の独自大盛り割増があればその値）。列見出しに `+¥{単価}` を表示。
  - 保存のため `InvoiceLine.quantity_large`（うち大盛り数）を追加（migration 0023）。

### レイアウト（ネスト見出し）
```
                    │    単 価     │ 大盛り │       │
 日 │ 曜 │ 摘要      │ ¥700 │ ¥650 │ +¥50  │ 追加  │  金額
────┼────┼──────────┼──────┼──────┼───────┼──────┼────────
  1 │ 水 │ お弁当    │      │  10  │   3   │      │ ¥6,650
  2 │ 木 │ お弁当    │   2  │   8  │   1   │ ¥200 │ ¥6,850
  3 │ 金 │          │      │      │       │      │
 …（対象月の全日付を並べる。受注の無い日は空行）…
────┼────┼──────────┼──────┼──────┼───────┼──────┼────────
 合計│    │          │  2個 │ 18個 │  4個  │¥追加 │ ¥charge
     3/3（金）【控除】返品                              △¥500
                              税抜 / 消費税 / 請求額（税込）
```
- **「大盛り」列・「追加」列はそれぞれ該当がある請求書だけ表示**（無ければ列ごと省略）。
- 基本単価列は弁当の総数（大盛り含む）を計上し、大盛り列に大盛り個数を別掲。上例 1日＝¥650×10個のうち3個が大盛り → `650×10 + 50×3 = 6,650`。

### 列の決まり方
- **単価列＝その請求書に含まれる弁当明細（OrderItem・単価>0・数量>0）の異なる単価の集合**を**降順**に並べる。データから動的に決める（固定値ではない）。
- セル＝その日・その単価の**注文数（食数）**。0 は空欄。
- **追加列**＝その日の「弁当以外」の税込金額合計（下記）。
- **金額列**＝その日の税込合計（＝弁当ぶん＋追加）。
- **合計行**＝各単価列の数量合計・追加列の金額合計・金額列に charge_total。

### 「弁当」と「追加」の振り分け
- **弁当（単価列へ）**：`InvoiceLine.order_item` あり **かつ** 単価>0 **かつ** 数量>0。
- **追加（追加列へ）**：上記以外のORDER行 ＝ 追加商品（`order_item` なし＝お茶等）／仕出し（`order_item` ありだが単価0・小計直接）。金額のみ加算。
- **控除（CREDIT調整）**：マトリクス下に従来どおり `発生日（曜）｜【控除】理由｜△金額` で1件ずつ。

### 対象期間・空行
- 月末締め（period あり）＝対象月の**全日付＋曜日**（受注の無い日も空行）。土日は淡色＋曜日赤字。
- 即時/スポット（period なし）＝受注のあった日だけ。

### 実装方針（保存は不変・表示だけ変更）
- モデル変更なし。`InvoiceLine`（`order_item`/`unit_price`/`quantity`/`amount`）から**表示時に集計**。
- `services.build_price_matrix(entries, period_start, period_end)` を新設。戻り値：
  - `price_columns`: 降順の単価リスト
  - `day_rows`: `[{date, weekday, has_order, cells_list:[qty…], large, extras, amount}]`（cells_list は price_columns と同じ並び）
  - `col_totals_list`: 各単価列の数量合計、`large_total`/`large_unit`/`has_large`、`extras_total`/`has_extras`、`charge_total`
  - `credit_rows`: 従来どおり
- 反映先：請求書PDF・請求書詳細・発行プレビューの3画面。

### PDFは1ページに収める（2段組み）
- 請求書PDF（A4縦）は**必ず1ページに収める**。日付行が多い月はそのままだと2ページになるため、**日付を左右2段組み**（1〜16日／17〜末日）にして高さを半減させる（日数16超で自動的に2段）。即時/スポット等の少日数は単段。
- 余白・フォント・行間を圧縮。単価別合計は2段組み時はテーブル下に1行でまとめて表示。
- テスト `test_pdf_full_month_fits_one_page` で「31日フル＋大盛り＋複数単価＋控除」でも1ページを保証。

### 既知の制約
- 単価の種類が多いとA4縦で横が詰まる。弁当は通常2〜5種で問題なし。**6種超は列が細くなる**（当面許容・必要なら要相談）。2段組み時は左右に列が並ぶため、単価種類が多いとさらに詰まる。

## ✅ ロック・不変性・二重請求防止（重要）

1. **1受注＝最大1枚の有効請求書**（`Order.invoice`）。発行済み受注は再選択不可（一覧/プレビューから除外）。
2. **発行(ISSUED)後は取込済みの受注・調整を編集/削除不可**：
   - 受注本体の編集/削除は請求済み（`order.is_invoiced`）ならブロック。
   - 調整の編集/削除は **その調整が請求書に取込済み（`billed_invoice` セット）** のときだけブロック。
   - ★ただし **請求済み受注へ「新規の」CREDIT調整を追加するのは許可**（＝後発クレーム。これが繰越の入口。適格返還請求書の実務）。追加時に「次回請求へ繰越されます」と明示する。
3. **請求書自体は発行後不変**。訂正は VOID → 作り直し（適格請求書の実務どおり）。
4. **VOIDでロック解除**：`order.invoice=NULL`・`credit.billed_invoice=NULL` に戻し、受注は再び未請求に。VOIDした請求書は履歴として残す（削除しない）。
5. 発行はトランザクション＋`select_for_update`で、同一受注が同時に2枚へ載る競合を防ぐ。

## ✅ 要決定事項（しょうへい確認）★これだけ先に決めたい

| # | 項目 | 案（推奨） | 影響 |
|---|---|---|---|
| A | **締め日** | 末日締め（顧客モデルがMONTH_END＝月末のみ） | 20日締め等が必要なら締め日フィールドを顧客に追加 |
| B | **支払期日(due_date)** | 翌月末払い（発行日から自動計算・空欄可） | 顧客ごとに違うなら顧客に支払サイト欄が要る |
| C | **請求書番号形式** | `INV-YYYYMM-####` 月内連番 | 既存の手発行番号と重複させたくなければ調整 |
| D | **消費税端数処理** | 請求書単位で `÷(1+税率)` `ROUND_DOWN`（納品書/領収書と同一） | 変えると帳票間で税額がズレる |
| E | **入金管理の範囲** | v2は手動 `PAID` フラグのみ。消込・督促・一部入金は**v3** | v2をどこまで太らせるか |
| F | **月末一括の粒度** | 「全MONTH_END顧客をまとめて発行（各社1枚ずつ生成）」を用意 | 1社ずつだけで十分なら簡素化 |

## ✅ 移行・非機能

- 追加：新規テーブル2つ（Invoice / InvoiceLine）＋既存への nullable FK 2本（Order.invoice / OrderAdjustment.billed_invoice）＋権限1フィールド。
- **既存の Order/OrderItem/OrderAdjustment/受注フロー/納品書/領収書に破壊的変更なし**（FK追加は全て nullable・後方互換）。
- 既存の未請求受注（過去データ）は `invoice=NULL`＝未請求として自然にv2へ乗る。過去分を遡って請求書化するかは運用判断（する場合はスポット発行でいつでも可）。
- パフォーマンス：月次一括は顧客×受注のN+1を `prefetch_related('items','extra_items','adjustments')` で回避。発行はトランザクション。

## ✅ テスト観点（v2）

- 集計：charge − credit = net、税割戻しが `pdf.py` と一致
- 日別明細：受注明細が納品日順で1行ずつ展開／数量・単価・金額が受注明細と一致／同日複数価格帯が複数行に分かれる／仕出し(数量0)は金額のみ／明細の金額合計＝charge_total
- ロック：発行後に order.invoice が張られる／再選択で除外される／二重請求が起きない
- 繰越：請求済み受注への後発CREDITが次回請求に単独控除行で乗る／二重取込されない
- CASH：請求書に一切載らない
- VOID：ロック解除で受注が未請求に戻る／履歴は残る
- 即時漏れ検知：`pending_immediate()` がIMMEDIATE未請求だけを返す
- 権限：`can_view_invoices` でメニュー出し分け

## ✅ 段階リリース（再掲）

- **v1（完了）**：受注調整台帳（登録・受注詳細反映・一覧・CSV・月次相殺サマリー）
- **v2（今回）**：請求書発行（Invoice/InvoiceLine・3導線・集計エンジン・繰越・適格請求書PDF・発行後ロック・請求状況可視化・即時漏れ検知）
- **v3（将来）**：入金消込・一部入金・督促・会計連携・電子帳簿保存アーカイブ
