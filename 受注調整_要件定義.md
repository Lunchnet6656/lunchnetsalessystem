# ✅ 目的・位置づけ（重要）

**最終目標は「請求書発行機能」の追加。** 本案件（受注調整＝返品・取消・返金・値引きの記録/相殺）は、その**第1弾**である。
したがって調整レコードは単体の記録で終わらせず、**将来の請求書がそのまま集計・相殺に使える台帳（赤伝／貸方伝票）**として設計する。

## 全体構想（3層）＝北極星

```
① Order（受注）        … 請求の元（何を届け、いくら請求するか）＝税込・不変
        ＋
② OrderAdjustment（調整）… 赤伝／貸方（返品・取消・値引きをいくら戻す/相殺するか）＝別レコード
        ↓ 集計
③ Invoice（請求書）★将来 … 顧客×期間で ①の合計 −（②のCREDIT合計）＋税 を発行・記録
```

- 本要件（v1）＝ **①に対する②の台帳を作る**。③が食える形に整える。
- ③（請求書発行）＝ **v2** で本実装。v1は③を塞がない設計だけ守る。

# ✅ 現状把握（コード確認済み）

- **請求書発行は未実装。** 帳票は領収書のみ（`orders/pdf.py` / `receipt.html`）。`Customer.invoice_timing`（即時/月末締め）は現状CSV表示にしか使われていない（`views.py:970`）。＝請求書は完全新規。
- 金額は**税込（内税）保持**。帳票は `合計 ÷ (1+税率)` で消費税を割り戻す（`orders/pdf.py:53`）。
- `OrderSettings.invoice_number = "T…"`（適格請求書発行事業者の登録番号）を保持済み＝**将来の請求書は適格請求書（インボイス制度）前提**。
- `Order`：`customer` / `order_date` / `delivery_date` / 明細（`items`・`extra_items`）/ `total`（税込プロパティ）。ステータス・金額保存・支払状況は無い。
- 支払いは `Customer.payment_method` ＋ `invoice_timing` で顧客単位。

# ✅ 重要前提（必ず守る）

- **元注文（Order/OrderItem）は書き換えない。** 調整は別レコードで立て、相殺で純額を出す（配達済み・請求済みの事実を保全／訂正の理由・日付・担当者を残す／1注文に複数クレーム可）。
- **1イベント（1返品/1取消/1クレーム）＝ 調整1レコード。** 1注文に調整N件。
- **金額はすべて税込（内税）。** 元注文とピタリ相殺でき、請求書の内税割戻しとも整合。
- **返金方法を厳密に区別する（請求書設計の要）：**
  - **請求相殺(CREDIT)** … 月末請求から差し引く → **将来の請求書に控除行として載る**。
  - **現金・振込返金(CASH)** … 既に返金済み → **請求書には載せない**（別集計）。
- v1は**社内記録のみ**（帳票は現状維持）。ただし後述の「将来を塞がない設計原則」を守る。

# ✅ スコープ

## v1でやること（今回）
- 受注への調整レコード登録（返品／取消／値引き／その他）
- 返金方法の区別：**請求相殺／現金・振込返金**
- 金額入力3モード：**明細単位（○個返品/取消）／注文まるごと取消／金額自由入力**
- 理由（テキスト・必須。クレーム内容もここ）
- 受注詳細への調整表示＋純額算出
- 調整一覧（絞り込み・合計・CSV）
- **月次×顧客 相殺サマリー**（＝将来の請求集計エンジンの前身・CSV）

## v1でやらないこと
- 請求書PDF・請求書レコードの発行（＝**v2**）
- クレームの独立管理（原因区分・再発防止）→ 理由テキストで代替
- 承認ワークフロー／決済ゲートウェイ連携／自動返金

# ✅ 実装詳細：モデル（v1）

## 1) orders.models.OrderAdjustment（受注調整＝ヘッダ・1イベント）

```python
class OrderAdjustment(models.Model):
    KIND_CHOICES = [('RETURN','返品'),('CANCEL','取消'),('DISCOUNT','値引き'),('OTHER','その他')]
    SETTLEMENT_CHOICES = [('CREDIT','請求相殺'),('CASH','現金・振込返金')]

    order        = FK(Order, on_delete=PROTECT, related_name='adjustments')  # 調整のある受注は削除不可
    kind         = CharField(choices=KIND_CHOICES)             # 種別
    settlement   = CharField(choices=SETTLEMENT_CHOICES)       # 返金方法（請求書に載る/載らないを分ける）
    amount       = DecimalField(10, 0)                         # 調整額（税込・円・≥0）
    reason       = TextField()                                 # 理由（必須）
    occurred_on  = DateField(default=今日)                      # 発生日（=どの請求期間で相殺するかの基準）
    created_by   = FK(AUTH_USER_MODEL, on_delete=SET_NULL, null=True)
    created_at   = DateTimeField(auto_now_add=True)
    updated_at   = DateTimeField(auto_now=True)
    # ── 将来(v2)接続用のseam。v1では未使用・追加は後日でも安全（nullable FK）──
    # billed_invoice = FK('Invoice', null=True, on_delete=PROTECT)  # 請求書に取込済みならロック対象
```

## 2) orders.models.OrderAdjustmentLine（受注調整明細・任意の子）

```python
class OrderAdjustmentLine(models.Model):
    adjustment       = FK(OrderAdjustment, on_delete=CASCADE, related_name='lines')
    order_item       = FK(OrderItem,      on_delete=SET_NULL, null=True, blank=True)
    order_extra_item = FK(OrderExtraItem, on_delete=SET_NULL, null=True, blank=True)
    label            = CharField()          # 商品名スナップショット
    quantity         = PositiveIntegerField()
    unit_price       = DecimalField(10, 0)  # 税込スナップショット
    subtotal         = DecimalField(10, 0)  # = unit_price × quantity（saveで自動）
```

## 3) orders.models.Order にフィールド追加（請求パターン）★受注画面で明記
```python
class Order(...):
    BILLING_PATTERN_CHOICES = [
        ('MONTH_END', '月末締め'),
        ('IMMEDIATE', '即時発行'),
    ]
    billing_pattern = CharField(max_length=10, choices=BILLING_PATTERN_CHOICES, default='MONTH_END')
    # 軸は「月末締め（定期）」か「即時発行（都度）」の2択＝顧客 invoice_timing と一致。
    # スポットまとめは v2 の発行時操作（複数受注を1枚に束ねる）で実現するため受注フラグは持たない。
    # 受注画面で必須選択。顧客の invoice_timing を初期値にプリフィル、注文ごとに上書き可。
```
- **なぜv1で入れるか**：請求書機能はv2だが、「どのパターンで請求したいか」は**受注時点でしか正確に取れない**。後付けだと過去データが空になるため、**フィールドと入力UIだけv1で先行**させる（記録の取りこぼし防止）。
- v1では記録・表示まで。発行状態との突合による「漏れ検知の実働」はv2（下記）。

## 4) Order にプロパティ追加（DB変更なし）
```python
adjustments_total   # 有効調整の合計（税込）
credit_total        # うちCREDIT（請求相殺）
cash_refund_total   # うちCASH（現金・振込）
net_total           # = total − adjustments_total（純額＝将来の請求対象額）
```

## 5) OrderUserMenuPermission に `can_view_adjustments`（既定True）を追加

# ✅ 金額の決まり方（3モード）

| モード | 明細行 | amount | 用途 |
|---|---|---|---|
| 明細単位 | 対象明細×返品数で生成 | 行合計を**自動計算** | 「弁当3個返品」 |
| まるごと取消 | 全明細を自動生成 | 全額**自動計算**（純額0・注文は残す） | 1注文取消 |
| 金額自由入力 | なし | **手入力** | 一律値引き・お詫び等 |

# ✅ 計算・バリデーション
- `amount > 0`
- 明細行がある場合 `amount` は行合計で自動算出（手入力禁止）
- **過剰返金の警告**：同一受注の調整累計 ＞ `total` のとき警告（ブロックせず許可）
- 税は税込保持。内訳は既存同様 `÷(1+税率)` で割戻し。

# ✅ 画面（URL）と挙動（v1）
`orders/urls.py` に追加（受注登録/編集は既存画面に追記）：
0. **受注登録/編集（既存 `order_create`/`order_edit` に追記）**：**請求パターン**（月末締め／即時発行）を**必須選択**。顧客選択時に顧客の既定（`invoice_timing`）を自動プリフィル・上書き可。支払いが請求書以外の顧客はセレクトを出さず支払い方法を固定表示。受注一覧/詳細にパターンを表示。
1. **受注詳細（既存に追記）**：調整セクション＋「受注額 → 相殺(−) → 純額」＋「調整を追加」
2. **登録** `<order pk>/adjustments/create/`：モード選択・返金方法・理由（必須）・発生日
3. **編集/削除** `adjustments/<pk>/edit/` `…/delete/`（権限者のみ）
4. **一覧** `adjustments/`：期間（発生日）・顧客・種別・返金方法で絞り込み、合計、CSV
5. **月次×顧客 相殺サマリー** `adjustments/monthly-summary/`：顧客ごと当月の CREDIT合計（＝請求から差引く額）／ CASH合計（＝返金実績）、CSV

# ✅ 将来（v2）＝請求書発行の設計想定と接続方針 ★見越し

v1の調整台帳を、そのまま請求書が食えるようにするための到達点を明記する（v1では作らないが、これを塞がない）。

## 想定モデル（v2）
```python
class Invoice(models.Model):                 # 請求書
    invoice_number   # 請求書番号（採番）
    customer FK
    period_start / period_end                # 対象期間（月末締めなら当月1日〜末日 / 即時なら単一注文）
    issue_date / due_date
    status           # DRAFT / ISSUED / PAID / VOID
    registration_number  # OrderSettings.invoice_number(T番号)のスナップショット＝適格請求書
    charge_total     # 期間内の受注合計（税込）
    credit_total     # 期間内のCREDIT調整合計（税込・控除）
    net_amount       # = charge_total − credit_total
    tax_amount       # 内税割戻し（÷1.08）
    notes

class InvoiceLine(models.Model):             # 請求明細（受注/調整を1行ずつ）
    invoice FK
    source_type      # ORDER / ADJUSTMENT
    order FK(null) / adjustment FK(null)
    label / amount   # 税込。調整(控除)はマイナス
```

## 発行パターン（軸は2つ・スポットは発行時操作）★重要
請求書は「固定の月次1枚」ではなく、**顧客の“まだ請求していない受注”を選んで1枚に束ねる選択駆動**で作る。

**軸は「月末締め（定期）」か「即時発行（都度）」の2択**。即時発行とスポットは同一エンジン（都度・手動で選んで発行）で、**即時＝1件、スポット＝N件**という束ねる件数の違いだけ。だからスポットは受注フラグではなく**発行時にN件を選ぶ操作**で表す。

| 発行の仕方 | 選び方 | 画面挙動 |
|---|---|---|
| ① 月末締め（定期） | 対象月[1日〜末日]の未請求受注を全部 | 期間指定→自動全選択→発行 |
| ② 即時発行（都度・1件） | その1受注だけ | 受注詳細から「この受注を請求」 |
| ③ スポットまとめ（都度・N件） | 任意の未請求受注を手で複数選択 | 一覧でチェック→まとめて発行 |

- ②と③は同じ「都度発行」。`billing_pattern=IMMEDIATE` の受注を、1件で出せば②、複数選んでまとめれば③。
- `Customer.invoice_timing`（月末/即時）と `Order.billing_pattern`（月末/即時）は**選択肢が一致**。顧客既定を受注にプリフィルし、受注ごとに上書き可。実発行は常に選択駆動。

## 請求の一意性・相殺の帰属
- **1受注は最大1枚の（有効な）請求書にのみ載る**（二重請求防止）。発行時に受注を請求書へロックする → `Order.invoice` FK（nullable・後付けで安全に追加）。
- CREDIT調整は、**その注文が載る請求書に控除行として付く**。
- **繰越クレジット**：既に請求済みの注文に後から発生したCREDIT調整は、**過去の発行済み請求書を書き換えず、次回の請求書に単独の控除行として繰り越す**（＝適格返還請求書／貸方の実務どおり）。
- **CASH返金は請求書に一切載せない**（別集計）。

## 集計ロジック（v2）
- 選択された受注群について **Σ受注合計（charge）− 紐づくCREDIT調整（credit）＝ net**、税は `÷(1+税率)` で割戻し。発行時点の税率を `Invoice.tax_rate` にスナップショット。
- **v1の「月次×顧客 相殺サマリー」は、この集計を“対象月の未請求受注”に限定した特殊ケース**。汎用クエリ（対象受注＋紐づくCREDIT調整の集計）で実装し、v2が①②③に再利用する。

## 即時発行の取りこぼし防止（v2）★
選択駆動の弱点＝「即時発行のつもりの受注が誰にも選ばれず放置」を潰す。
- `Order.billing_pattern`（v1で記録）と `Order.invoice`（v2の請求済み判定）を突合。
- 受注ダッシュボードに **「要即時請求」ウィジェット**：`billing_pattern=IMMEDIATE` かつ未請求の受注を、**件数バッジ付き**で一覧。
- 受注一覧に **請求状況（未請求／請求済み）列**。
- （任意）即時受注の作成直後に「続けて請求書を発行しますか？」導線。
- ※ v1では請求発行状態が無いため“漏れ検知”は未成立。**v1でパターンを記録 → v2で発行状態と突合して漏れゼロ化**の2段構え。

## v1で守る「将来を塞がない」設計原則
1. **CREDIT/CASHの区別を厳密に。** 請求書に載る/載らないの唯一の判定軸になる。
2. **occurred_on を必ず持つ。** 「どの請求期間で相殺するか」の帰属キー。
3. **金額は税込一本。** 請求書の内税割戻しと同一基準。
4. **元注文は不変・調整は別レコード。** 請求書は「注文＋調整」を参照集計するだけで済む。
5. **後付けの `billed_invoice` FK（nullable）で発行後ロックできる形にする。** v1では未追加でよい（nullable FK追加は安全マイグレーション）。請求書発行時に取込済みの注文・調整を編集不可にする布石。
6. **税率スナップショットの検討事項**：現状は `OrderSettings.tax_rate` 全体値（軽減税率8%固定運用）。税率が変わる場合は請求書に発行時点の税率を残す必要 → v2で `Invoice.tax_rate` にスナップショット（v1のデータには影響しない）。

# ✅ 移行・非機能（v1）
- 新規テーブル2つ＋権限1フィールドのみ。**既存 Order/OrderItem/受注フロー/帳票に変更なし＝後方互換・低リスク。**
- マイグレーションはテーブル追加＋既定値ありフィールド追加で安全。

# ✅ 段階リリース
- **v1（今回）**：受注調整台帳（登録・受注詳細反映・一覧・CSV・月次相殺サマリー）
- **v2（本命）**：請求書発行（Invoice/InvoiceLineモデル・顧客×期間集計・適格請求書PDF・発行後ロック）
- **v3（将来）**：入金消込／督促／原因区分の集計
